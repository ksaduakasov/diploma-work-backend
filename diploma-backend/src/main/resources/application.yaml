spring:
  application:
    name: diploma-work

  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:postgresql://localhost:5432/diploma
    username: test
    password: password
    driverClassName: org.postgresql.Driver
